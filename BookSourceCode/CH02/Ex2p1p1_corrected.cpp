#include <cmath>
#include <iostream>
using std::cout;
using std::sqrt;
using std::endl;
int main() {
  int x = 3;
  int y = 4;
  double w = x / 2;
  char ch = 'a' + 'b';
  int z;
  if (x = y) {
    z = x++;
  } else {
    z = y++;
  }
  cout << z << "***" << sqrt(z) << endl;
}
