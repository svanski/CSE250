#include <cmath>
#include <iostream>
using std::cout;
using std::sqrt;
using std::endl;
int main() {
  int x = 3.45;
  double w = x / 2;
  char ch = 'a' + 'b';
  if (x = y) {
    int z = x++;
    else
      int z = y++;
  }
  cout << z << "***" << sqrt(z) << endl;
}
